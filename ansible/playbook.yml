---
# SLES 15 Hardening Playbook
# Placeholder - replace with actual hardening playbooks

- name: Apply SLES 15 Security Hardening
  hosts: localhost
  connection: local
  become: yes
  gather_facts: no

  vars:
    max_fail_pct: 25

  tasks:
    - name: Display system information
      debug:
        msg: |
          Hardening started on {{ inventory_hostname }}
          Hardening level: {{ hardening_level }}
